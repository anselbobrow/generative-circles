import Nucleus from "./Nucleus";

export default class NightSky {
    private allNuclei: Nucleus[];

    constructor(pattern: string) {
        // generate a pattern using a pattern generating method
        // store the results in allNuclei
    }

    /* Returns a set of nuclei following the NightSky's
    * specified pattern that can be rendered */
    getNucleusMap() {
        return this.allNuclei;
    }
}